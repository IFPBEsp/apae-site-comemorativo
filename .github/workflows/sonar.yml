name: SonarQube Test Pipeline

on:
  push:
    branches:
      - test/sonarqube-pipeline

permissions:
  contents: read

jobs:
  sonar-frontend:
    name: Frontend Analysis (Node)
    uses: IFPBEsp/APAE-INFRA/.github/workflows/reusable-sonarqube-node.yml@5bbe737d15fb6284924dd54f38743dfdd2c9545d
    with:
      project-key: "teste-apae_site-comemorativo"
      organization: "teste-apae"
      working-directory: "."
      node-version: "22"
      sonar-host-url: "https://sonarcloud.io"
      run-tests: true
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
