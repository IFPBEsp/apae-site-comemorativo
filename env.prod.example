# Configurações de Produção - APAE Site Comemorativo
# Copie este arquivo para .env e configure as variáveis conforme necessário

# Configurações do Banco de Dados PostgreSQL
POSTGRES_DB=apae_db_prod
POSTGRES_USER=apae_user_prod
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD_IN_PRODUCTION

# URL de conexão com o banco de dados (usada pela aplicação)
DATABASE_URL=postgresql://apae_user_prod:CHANGE_THIS_PASSWORD_IN_PRODUCTION@postgres:5432/apae_db_prod

# Configurações do MinIO
MINIO_ROOT_USER=minioadmin_prod
MINIO_ROOT_PASSWORD=CHANGE_THIS_PASSWORD_IN_PRODUCTION
MINIO_BROWSER_REDIRECT_URL=https://yourdomain.com/minio

# Configurações da aplicação
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Configurações do MinIO para a aplicação
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin_prod
MINIO_SECRET_KEY=CHANGE_THIS_PASSWORD_IN_PRODUCTION
MINIO_BUCKET_NAME=apae-files-prod

# Configurações de autenticação
JWT_SECRET=CHANGE_THIS_JWT_SECRET_IN_PRODUCTION

# Configurações de domínio
NEXT_PUBLIC_APP_URL=https://yourdomain.com
NEXT_PUBLIC_MINIO_URL=https://yourdomain.com/minio

# Configurações de email (se necessário)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Configurações de backup
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
